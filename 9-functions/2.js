// Learn the meanings of each of these terms regarding methods:
  //Method signature/name -> what you call the function
  //Parameters -> the inputs that are required for the function to run
  //Arguments (how are parameters and arguments different?) -> the inputs that are passed into the function to make it run
  //Function call -> how you call the function so that it performs the javascript code written inside
  //Return values -> the output of a function
  //Method body -> the javascript performed within the function itself
